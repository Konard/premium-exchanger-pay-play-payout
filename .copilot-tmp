# composer.lock is now generated and dependencies are installed in the Docker environment.
# Next, update the Dockerfile to COPY composer.lock before running composer install for reproducible builds.
# Then, run PHPUnit with coverage enabled inside the container.
